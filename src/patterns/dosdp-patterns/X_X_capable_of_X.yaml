# options: [min(20),dir(cl),trim(true),base('http://purl.obolibrary.org/obo/cl'),generalize_properties(true),max_and_cardinality(4),max_class_signature(5),annotations([ann(exact_synonym,oio:hasExactSynonym,0.05),ann(related_synonym,oio:hasRelatedSynonym,0.05)])]
pattern_name: X X capable of X
pattern_iri: http://purl.obolibrary.org/obo/cl/X_X_capable_of_X

description: >-
  A cell type that is capable of detecting a stimulus that is involved in sensory pereception (eg a receptor cell).

  Examples: [neuronal receptor cell](http://purl.obolibrary.org/obo/CL_0000006)
classes: 
  detection of stimulus involved in sensory perception: "GO:0050906"


relations: 
  capable of: "RO:0002215"


vars:
  v0: "'native cell'"
  v1: "'native cell'"
  v2: "'detection of stimulus involved in sensory perception'"

name:
  # Could not induce name, using default
  text: "%s %s capable of %s"
  vars:
    - v0
    - v1
    - v2

def:
  # Could not induce def, using default
  text: "Any %s that %s and capable of a %s"
  vars:
    - v0
    - v1
    - v2

annotationProperties:
  exact_synonym: "oio:hasExactSynonym"
  related_synonym: "oio:hasRelatedSynonym"

# could not infer annotations


equivalentTo:
  text: "%s and %s and ('capable of' some %s)"
  vars:
    - v0
    - v1
    - v2
